name: Generate sample detail pages

on:
  push:
    branches: [main]
    paths:
      - content/portfolio.json
      - scripts/generate-sample-pages.mjs

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/generate-sample-pages.mjs
      - name: Commit generated pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add samples/*/index.html
          git diff --cached --quiet || git commit -m "Generate sample detail pages"
          git push
